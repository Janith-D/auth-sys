{
  "name": "auth-sys",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd auth-backend && npm run dev",
    "build": "cd auth-backend && npm run build",
    "start": "cd auth-backend && npm start",
    "dev:frontend": "cd auth-frontend && npm run dev"
  }
}
